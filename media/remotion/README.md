# Project visuals

This small Remotion project creates the illustrated tour plus the statistics image used by the main README. The numbers come from the repository checks. Provider boxes are illustrations, not screenshots.

Install the dependencies with `npm install`. Render all committed media with `npm run render:poster`, `npm run render:stats`, then `npm run render:tour`. Run `npm run typecheck` to check the project source.
